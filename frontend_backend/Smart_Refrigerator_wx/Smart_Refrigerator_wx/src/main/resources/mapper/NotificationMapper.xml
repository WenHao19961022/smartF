<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smartfridge.mapper.NotificationMapper">

    <select id="getUnreadList" resultType="com.smartfridge.entity.Notification">
        SELECT * FROM notification
        WHERE openid = #{openid} AND is_read = 0
        ORDER BY create_time DESC
    </select>

    <update id="markAllRead">
        UPDATE notification SET is_read = 1 WHERE openid = #{openid}
    </update>

</mapper>